# Neural Network From Scratch — Dependencies
# Only NumPy for the NN, sklearn for validation only

numpy>=1.24.0
matplotlib>=3.8.0
seaborn>=0.13.0
scikit-learn>=1.3.0      # validation / comparison only — NOT used in NN code
pytest>=7.4.0
pytest-cov>=4.1.0
click>=8.1.0
tabulate>=0.9.0
tqdm>=4.66.0
